# IELTS Speaking Bot

Telegram-бот для автоматической оценки устной речи по шкале IELTS.
Студент отправляет голосовое сообщение на английском — бот даёт развёрнутую оценку и советы.

---

## Как это работает

```
Студент отправляет голосовое (OGG)
            ↓
    FFmpeg конвертирует в MP3 (16kHz, mono)
            ↓
    OpenAI Whisper-1 → текстовый транскрипт
            ↓
    GPT-4o анализирует по критериям IELTS
            ↓
    Бот отвечает подробной оценкой
```

---

## Что получает студент в ответ

- **Band-баллы** по 4 критериям: Fluency & Coherence, Lexical Resource, Grammatical Range & Accuracy, Pronunciation
- **Итоговый балл** (среднее, округлённое до 0.5)
- **Конкретные ошибки** с исправлениями
- **Улучшенный вариант** ответа (Band 7+)
- **Советы** по улучшению

---

## Что получает учитель в ответ

Учительский отчёт компактнее и включает расширенный анализ для работы с учеником:

| Секция | Описание |
|---|---|
| **IELTS Scores** | Баллы по 4 критериям + итоговый, одна строка обоснования на каждый |
| **Authenticity Check** | Зачитал ли ученик с бумаги? Использовал ли ИИ для генерации? Вердикт + причина |
| **Top 3 Grammar Mistakes** | Три наиболее значимых грамматических ошибки с исправлением и правилом |
| **Top 10 Vocabulary Misuses** | Слова, использованные некорректно или неточно, с заменами для данного контекста |
| **Idea Development** | Насколько чётко сформулированы идеи, есть ли поддержка примерами, логика изложения |

---

## Стек технологий

| Компонент        | Технология                    |
|------------------|-------------------------------|
| Telegram-фреймворк | aiogram 3.x                 |
| Транскрипция     | OpenAI Whisper-1              |
| Оценка речи      | OpenAI GPT-4o                 |
| Конвертация аудио | FFmpeg                       |
| Язык             | Python 3.11+                  |
| Хостинг          | DigitalOcean Droplet (Ubuntu) |

---

## Структура проекта

```
ielts-bot/
├── bot/
│   ├── bot.py              # Роутинг: личный чат (роль) и группа (модерация)
│   ├── config.py           # Конфигурация (читает переменные окружения)
│   ├── roles.py            # Определение роли пользователя (teacher / student / outsider)
│   └── handlers/
│       ├── student.py      # Обработчик для учеников: IELTS-оценка
│       ├── teacher.py      # Обработчик для учителей: расширенный анализ
│       └── group.py        # Модерация группы: мат, ссылки, флуд, дубли
├── chatgpt_api/
│   ├── __init__.py
│   └── gpt.py              # Интеграция с OpenAI (Whisper + GPT-4o, два промпта)
├── .env.example            # Шаблон переменных окружения
├── .gitignore
├── requirements.txt        # Python-зависимости
├── ielts-bot.service       # systemd-юнит для автозапуска на сервере
├── README.md
├── DEPLOY.md               # Инструкция по деплою на DigitalOcean
└── TESTING.md              # Руководство по тестированию
```

---

## Переменные окружения

Создай файл `.env` на сервере (по шаблону `.env.example`):

```env
BOT_TOKEN=your_bot_token_here            # Токен бота от @BotFather
OPENAI_API_KEY=sk-...                    # Ключ OpenAI API
VIDEO_SAVING_PATH=/opt/ielts-bot/savings # Папка для аудио
GROUP_ID=-1001234567890                  # ID группы (учителя = admin группы)
```

> **Важно:** файл `.env` никогда не коммитится в git.

---

## Локальный запуск (для разработки)

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd ielts-bot

# 2. Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Создать .env файл
cp .env.example .env
# Заполнить .env своими ключами

# 5. Убедиться, что ffmpeg установлен
ffmpeg -version

# 6. Запустить бота
python -m bot.bot
```

---

## API расходы

| Операция | Модель | Цена |
|---|---|---|
| Транскрипция | whisper-1 | $0.006 / минута аудио |
| Оценка (ученик) | gpt-4o | ~$0.008 / запрос |
| Оценка (учитель, расширенная) | gpt-4o | ~$0.012 / запрос (промпт длиннее) |
| **Итого на запрос** | | **~$0.01–0.02 / запрос** |

---

## Деплой на сервер

Подробная инструкция: **[DEPLOY.md](./DEPLOY.md)**

---

## Требования к серверу

- Ubuntu 22.04 LTS (или 24.04)
- Python 3.11+
- ffmpeg
- 1 GB RAM минимум (рекомендуется 2 GB)
- 20 GB SSD

---

## Команды бота

| Команда | Кому | Описание |
|---|---|---|
| `/start` | всем | Приветствие с описанием доступных функций |
| `/help` | всем | То же, что `/start` |
| голосовое | ученик | Краткая оценка IELTS по 4 критериям |
| голосовое | учитель | Расширенный анализ: IELTS + аутентичность + грамматика + словарь + идеи |
